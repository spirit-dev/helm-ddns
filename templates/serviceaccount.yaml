{{- if .Values.serviceAccount.create -}}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ddns.fullname" . }}-external-cname
  labels:
    {{- include "ddns.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "ddns.fullname" . }}-external-cname
rules:
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses"]
    verbs: ["get", "create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "ddns.fullname" . }}-external-cname
subjects:
  - kind: ServiceAccount
    name: {{ include "ddns.fullname" . }}-external-cname
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: {{ include "ddns.fullname" . }}-external-cname
  apiGroup: rbac.authorization.k8s.io
{{- end }}
